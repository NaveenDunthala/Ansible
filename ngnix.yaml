- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx service
    ansible.builtin.service:
      name: nginx start
      state: started
      enabled: yes